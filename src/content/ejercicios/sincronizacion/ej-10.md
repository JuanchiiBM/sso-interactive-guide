---
titulo: Compilador y notificador (productor-consumidor)
tema: parcial-1/sincronizacion
fuente: { guia: Guía de Ejercicios – Sincronización (v.2C2026), numero: 10 }
tipo: codigo
dificultad: media
tags: [semaforos, productor-consumidor, buffer]
semaforos:
- etiqueta: a. Lista sin límite
  procesos:
  - nombre: Compilador
    instancias: 2
    codigo: |
      while(TRUE){
        id_programa = obtener_nuevo_programa();
        r = compilar_programa(id_programa);
        depositar_resultado(r, lista);
      }
  - nombre: Notificador
    instancias: 1
    codigo: |
      while(TRUE){
        r2 = retirar_resultado(lista);
        enviar_email(r2);
      }
  acciones:
    id_programa = obtener_nuevo_programa(): {}
    r = compilar_programa(id_programa): {}
    depositar_resultado(r, lista):
      recursos:
      - lista
      efecto:
        resultados: 1
    r2 = retirar_resultado(lista):
      recursos:
      - lista
      efecto:
        resultados: -1
    enviar_email(r2): {}
  variables:
    resultados: 0
  tests:
  - tipo: exclusion
    recurso: lista
    nombre: Mutua exclusión sobre la lista
  - tipo: rango
    variable: resultados
    min: 0
    nombre: Nunca se retira de una lista vacía
    motivo: Hay una intercalación en la que el notificador retira un resultado cuando la lista está vacía.
  - tipo: sin-deadlock
  - tipo: todas-ejecutan
  solucion: |
    semaphore mutex = 1, hay_resultados = 0;

    void Compilador() {
      while(TRUE){
        id_programa = obtener_nuevo_programa();
        r = compilar_programa(id_programa);
        wait(mutex);
        depositar_resultado(r, lista);
        signal(mutex);
        signal(hay_resultados);
      }
    }

    void Notificador() {
      while(TRUE){
        wait(hay_resultados);
        wait(mutex);
        r2 = retirar_resultado(lista);
        signal(mutex);
        enviar_email(r2);
      }
    }
  justificacion: |
    Es el **productor-consumidor** con buffer ilimitado:

    | Semáforo         | Tipo     | Valor inicial | Para qué                                        |
    | ---------------- | -------- | ------------- | ----------------------------------------------- |
    | `mutex`          | mutex    | 1             | la lista es compartida                          |
    | `hay_resultados` | contador | 0             | cuenta resultados; el notificador espera si no hay |

    Regla de la cátedra: para **contar instancias** en una colección, `signal` **después** de depositar
    y `wait` **antes** de consumir, inicializado en 0. Compilar queda fuera del mutex: no toca la lista.
- etiqueta: b. Lista con límite M
  nota: |
    Podés usar la constante `M` al inicializar (`semaphore x = M;`). Para correr los tests se toma **M = 2**.
  procesos:
  - nombre: Compilador
    instancias: 2
    codigo: |
      while(TRUE){
        id_programa = obtener_nuevo_programa();
        r = compilar_programa(id_programa);
        depositar_resultado(r, lista);
      }
  - nombre: Notificador
    instancias: 1
    codigo: |
      while(TRUE){
        r2 = retirar_resultado(lista);
        enviar_email(r2);
      }
  acciones:
    id_programa = obtener_nuevo_programa(): {}
    r = compilar_programa(id_programa): {}
    depositar_resultado(r, lista):
      recursos:
      - lista
      efecto:
        resultados: 1
    r2 = retirar_resultado(lista):
      recursos:
      - lista
      efecto:
        resultados: -1
    enviar_email(r2): {}
  variables:
    resultados: 0
  constantes:
    M: 2
  tests:
  - tipo: exclusion
    recurso: lista
    nombre: Mutua exclusión sobre la lista
  - tipo: rango
    variable: resultados
    min: 0
    nombre: Nunca se retira de una lista vacía
    motivo: Hay una intercalación en la que el notificador retira un resultado cuando la lista está vacía.
  - tipo: rango
    variable: resultados
    max: 2
    nombre: Nunca hay más de M resultados en la lista
    motivo: Hay una intercalación en la que un compilador deposita con la lista ya llena (M resultados).
  - tipo: sin-deadlock
  - tipo: todas-ejecutan
  solucion: |
    semaphore mutex = 1, hay_resultados = 0, lugares = M;

    void Compilador() {
      while(TRUE){
        id_programa = obtener_nuevo_programa();
        r = compilar_programa(id_programa);
        wait(lugares);
        wait(mutex);
        depositar_resultado(r, lista);
        signal(mutex);
        signal(hay_resultados);
      }
    }

    void Notificador() {
      while(TRUE){
        wait(hay_resultados);
        wait(mutex);
        r2 = retirar_resultado(lista);
        signal(mutex);
        signal(lugares);
        enviar_email(r2);
      }
    }
  justificacion: |-
    Igual que (a) más un **tope de M**: `lugares`, contador inicializado en **M**. Regla de la cátedra:
    para fijar un tope, `wait` **inmediatamente antes** de depositar.

    ⚠️ **El orden de los `wait` importa**: si el compilador hace `wait(mutex)` antes de `wait(lugares)`
    y la lista está llena, se bloquea **con el mutex tomado**; el notificador no puede entrar a retirar
    y quedan todos trabados (deadlock). El orden de los `signal`, en cambio, no importa.
---

Suponga que un proceso tiene por tarea compilar un conjunto de programas y luego enviar el resultado de cada compilación por email al encargado de ese proyecto. Dicho proceso está organizado de la siguiente manera: N hilos de kernel compilan cada uno un programa distinto, y luego cada uno de ellos depositan en una lista (compartida para todo el proceso) el resultado; por otro lado, un hilo de kernel retira los resultados de las compilaciones y manda un email por cada uno de ellos.

**Estructura compartida:** `lista // Lista de resultados de compilaciones`

<div class="codigo-columnas">

**KLT Compilador (n instancias)**

```c
while (TRUE){
  id_programa = obtener_nuevo_programa();
  r = compilar_programa(id_programa);
  depositar_resultado(r, lista);
}
```

**KLT Notificador (1)**

```c
while (TRUE){
  r2 = retirar_resultado(lista);
  enviar_email(r2);
}
```

</div>

Asumiendo que la cantidad de programas a compilar es infinita, sincronice dicho código mediante el uso de semáforos para lograr un correcto uso de los recursos bajo los siguientes contextos:

- a. Asumiendo que la lista no tiene límite de tamaño.
- b. Asumiendo que la lista tiene un límite de M resultados como máximo.
