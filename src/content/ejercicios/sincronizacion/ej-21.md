---
titulo: 'Red social "Z": errores de sincronización'
tema: parcial-1/sincronizacion
fuente:
  guia: 1° Recuperatorio 2C2025
  numero: 21
tipo: codigo
dificultad: parcial
tags:
- semaforos
- errores
- productor-consumidor
- parcial
preguntas:
- enunciado: ¿Por qué el sistema "frecuentemente deja de funcionar hasta que se reinicia el Analizador"?
  opciones:
  - texto: Porque mostrarEnPantalla() queda dentro de la sección crítica del usuario
    explicacion: 'Eso lo hace más lento, pero no lo traba: el mutex se libera igual.'
  - texto: Porque el Analizador toma mutexPosts y después espera hayPosts con la cola vacía
    explicacion: 'Correcta: retiene el mutex esperando algo que solo un usuario (que necesita el mutex)
      puede dar.'
  - texto: Porque el usuario hace signal(hayPosts) antes de soltar el mutex de la cola
    explicacion: 'El orden de los signal no importa: no puede trabar a nadie.'
  - texto: Porque guardarEnDisco() se ejecuta sin exclusión mutua sobre la cola de posts
    explicacion: 'guardarEnDisco() no toca la cola: no necesita el mutex.'
  correcta: 1
  justificacion: |
    Es el error clásico de **orden de los `wait`**: el Analizador hace `wait(mutexPosts)` y **después**
    `wait(hayPosts)`. Si la cola está vacía, se bloquea **con el mutex tomado**. Los usuarios, para postear,
    necesitan ese mutex: quedan todos bloqueados en `wait(mutexPosts)` y nadie puede hacer el
    `signal(hayPosts)` que despertaría al Analizador → **deadlock**, hasta que alguien lo reinicia.

    Regla de la cátedra: el semáforo contador va **antes** que el mutex (primero "¿hay algo?", después
    "entro a la estructura").
semaforos:
- etiqueta: Corregí la sincronización
  nota: |
    El editor trae la sincronización del enunciado. **Mové** los `wait`/`signal` para que no haya deadlock y las secciones críticas sean lo más cortas posible (sin tocar las demás líneas). Para los tests hay 2 usuarios.
  inicial: |
    semaphore hayPosts = 0, mutexPosts = 1;
  procesos:
  - nombre: Usuario
    instancias: 2
    codigo: |
      While(1) {
        wait(mutexPosts);
        post = generarPost();
        postear(post, postsNuevos);
        signal(hayPosts);
        mostrarEnPantalla(post);
        signal(mutexPosts);
      }
  - nombre: Analizador
    instancias: 1
    codigo: |
      While(1) {
        wait(mutexPosts);
        wait(hayPosts);
        post = obtenerPost(postsNuevos);
        resultado = procesar(post);
        guardarEnDisco(resultado);
        signal(mutexPosts);
      }
  variables:
    posts: 0
  acciones:
    post = generarPost(): {}
    postear(post, postsNuevos):
      recursos:
      - postsNuevos
      efecto:
        posts: 1
    mostrarEnPantalla(post): {}
    post = obtenerPost(postsNuevos):
      recursos:
      - postsNuevos
      efecto:
        posts: -1
    resultado = procesar(post): {}
    guardarEnDisco(resultado): {}
  tests:
  - tipo: exclusion
    recurso: postsNuevos
    nombre: Mutua exclusión sobre la cola postsNuevos
  - tipo: rango
    variable: posts
    min: 0
    nombre: El analizador nunca saca de una cola vacía
    motivo: Hay una intercalación en la que el analizador obtiene un post de la cola vacía.
  - tipo: sin-deadlock
  - tipo: simultaneas
    acciones:
    - post = generarPost()
    - postear(post, postsNuevos)
    nombre: Un usuario puede generar su post mientras otro postea
    motivo: 'Generar el post quedó dentro de la sección crítica: un usuario espera el mutex para algo
      que no toca la cola.'
  - tipo: simultaneas
    acciones:
    - mostrarEnPantalla(post)
    - postear(post, postsNuevos)
    nombre: Un usuario puede mostrar en pantalla mientras otro postea
    motivo: 'mostrarEnPantalla quedó dentro de la sección crítica: no toca la cola compartida.'
  - tipo: simultaneas
    acciones:
    - guardarEnDisco(resultado)
    - postear(post, postsNuevos)
    nombre: El analizador puede guardar en disco mientras un usuario postea
    motivo: El analizador retiene el mutex durante una operación larga en disco que no toca la cola.
  solucion: |
    semaphore hayPosts = 0, mutexPosts = 1;

    void Usuario() {
      While(1) {
        post = generarPost();
        wait(mutexPosts);
        postear(post, postsNuevos);
        signal(mutexPosts);
        signal(hayPosts);
        mostrarEnPantalla(post);
      }
    }

    void Analizador() {
      While(1) {
        wait(hayPosts);
        wait(mutexPosts);
        post = obtenerPost(postsNuevos);
        signal(mutexPosts);
        resultado = procesar(post);
        guardarEnDisco(resultado);
      }
    }
  justificacion: |
    Los cuatro errores que marca la resolución oficial, y cómo se ven en los tests:

    1. **Analizador — orden de los `wait`** (el grave): primero `wait(hayPosts)`, después el mutex. Si no,
       deadlock con la cola vacía.
    2. **Usuario — `wait(mutexPosts)` antes de `generarPost()`**: generar el post no toca la cola; va antes del
       mutex.
    3. **Usuario — `signal(mutexPosts)` después de `mostrarEnPantalla()`**: se libera apenas se postea.
    4. **Analizador — `signal(mutexPosts)` al final**: se libera apenas se obtiene el post; `procesar` y
       `guardarEnDisco` son largos y no tocan la cola.

    Los errores 2 a 4 no rompen la corrección, pero hacen que el sistema sea "más lento de lo esperado":
    por eso son tests de **concurrencia posible** (que dos cosas puedan ocurrir a la vez).
---

El siguiente pseudocódigo simula la interacción de N usuarios en la red social "Z" y un proceso analizador que toma cada uno de los posts generados para analizarlos. A pesar de estar sincronizado, el sistema **se desempeña más lento de lo esperado** y **frecuentemente deja de funcionar** hasta que el administrador reinicia el proceso Analizador.

Variables compartidas: `postsNuevos` → cola que contiene los nuevos posts a analizar.  
Semáforos: `hayPosts` → semáforo contador inicializado en 0 · `mutexPosts` → semáforo mutex.

<div class="codigo-columnas">

**Usuario (N instancias)**

```c
While(1) {
  wait(mutexPosts);
  post = generarPost();
  postear(post, postsNuevos);
  signal(hayPosts);
  mostrarEnPantalla(post);
  signal(mutexPosts);
}
```

**Analizador (1 instancia)**

```c
While(1) {
  wait(mutexPosts);
  wait(hayPosts);
  post = obtenerPost(postsNuevos);
  resultado = procesar(post);
  guardarEnDisco(resultado);
  signal(mutexPosts);
}
```

</div>

Encuentre errores y/o mejoras en la sincronización planteada.
